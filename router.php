<?php
// Placeholder router for the PHP built-in server. Returning false tells the
// server to fall back to its default static-file handling, so behaviour is
// unchanged today. This stub exists so future SEO tasks can add routing here
// without a separate setup step.
return false;
